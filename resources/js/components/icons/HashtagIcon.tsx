import { Copy, Hash } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function HashTagIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Hash {...rest} />
    )
}
