import { Copy, Home } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function HomeIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Home {...rest} />
    )
}

