import { Copy, Plus, Repeat, Star } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function StarIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Star {...rest} />
    )
}

