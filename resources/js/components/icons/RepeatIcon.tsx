import { Copy, Plus, Repeat } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function RepeatIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Repeat {...rest} />
    )
}

