import { Copy, Plus, Repeat, Search } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function SearchIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Search {...rest} />
    )
}

