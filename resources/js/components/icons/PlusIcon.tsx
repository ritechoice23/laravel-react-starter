import { Copy, Plus } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function PlusIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Plus {...rest} />
    )
}

