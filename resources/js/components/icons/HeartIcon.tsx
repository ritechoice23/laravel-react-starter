import { Copy, Hash, Heart } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function HeartIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Heart {...rest} />
    )
}

