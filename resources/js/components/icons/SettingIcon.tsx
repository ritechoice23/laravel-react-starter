import { Copy, Plus, Repeat, Settings } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function SettingIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <Settings {...rest} />
    )
}

