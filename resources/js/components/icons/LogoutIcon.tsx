import { Copy, LogOut } from 'lucide-react'
import React, { SVGProps } from 'react'

export default function LogOutIcon({ ...rest }: SVGProps<SVGSVGElement>) {
    return (
        <LogOut {...rest} />
    )
}

